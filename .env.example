# Frontend-only mode (no backend): used when calling FMP directly
VITE_FMP_API_KEY=your_fmp_api_key_here

# Multi-user mode: backend URL (set when deploying frontend separately)
# VITE_API_URL=https://your-backend.example.com

# Optional: URL to JSON file with ticker→CUSIP map
# VITE_CUSIP_DATA_URL=https://example.com/cusips.json

# Supabase (for user accounts, watchlist sync)
# Get these from: Supabase project > Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
