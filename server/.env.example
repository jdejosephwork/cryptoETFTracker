# Required for full FMP data. Omit to use knowledge-base fallbacks only.
FMP_API_KEY=your_fmp_api_key_here

# Optional: URL to JSON with ticker→CUSIP map. Format: {"IBIT":"46438F101",...}
# CUSIP_DATA_URL=https://example.com/cusips.json

# Optional: protect POST /api/sync. If set, requests must include: Authorization: Bearer <key>
# SYNC_API_KEY=your_secret_sync_key

# Server port (default 3001)
# PORT=3001

# Supabase (required for Stripe Pro subscriptions)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Stripe Pro subscriptions (optional - omit to disable)
# STRIPE_SECRET_KEY=sk_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_ID=price_...  (create a recurring price in Stripe Dashboard)
